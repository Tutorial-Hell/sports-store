Feature - Auth Layout

Create a new folder under the app folder called [auth] create routes for SignIn and SignOut. title of the pages should reflect the route path

create a file in the [auth] folder called layout.tsx

copy the RootLayout function from root/layout.tsx but change the function name to AuthLayout

The AuthLayout function should return a layout in which the content is centered on the page vertically and horizontally

the SgnIN content consists of a card containing the logo and the signin prompts

